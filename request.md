## 应用开发：
这是一个功能类似Vector VX1000通过仿真口读写内存变量采集和标定MCU 实现和 CANape 软件的功能类似的软件工程开发；
整个项目用C语言开发，保证执行速度和最小系统资源占用；
框架为：
软件的UI界面开发可以选择用VisualStudio
主程序运行起来，通过动态的加载.dll 文件实现各种功能扩展；
后续如果需要开发新的功能，可以直接开发.dll 文件即可实现功能扩展；
BMAD管理和规划整个开发、测试流程；全程自动化，不再需要用户反复确认指令是否允许执行；

## 软件功能需要实现：
1、UI界面实现，最好用Microsoft visual studio 原生支持的UI工具实现；
2、UI界面实现.elf 文件选择（需要动态监控/加载，一旦用户重新编译后，软件能立即感知，并弹窗提示用户elf更新，刷新全部当前全局变量地址，找不到的变量弹窗提示用户是否忽略），变量解析，需要能将全局变量、结构体展开，结合变量的数据类型，获取变量的绝对地址；
3、UI界面需要有连接、断开连接、数据采集等按键，方便用户采集、log数据、离线回放数据，数据可以另存为文件（类似CANape .mf4格式数据，或者csv 格式数据）；
4、UI界面支持用户添加新窗口（波形/数值等类型窗口，可以通过C语言编译成单独dll 模块实现），窗口里支持用户添加变量（需要根据加载的elf文件支持快速模糊搜索），绘制变量的实时曲线或者用户修改变量值；
5、模块化开发，一个功能一个.dll 实现，这样搭建框架后，后续扩展新功能更方便，新功能单元测试也更容易实现；
6、编译器在：C:\Program Files\Microsoft Visual Studio\18\Community
7、code 文件夹用于开发整个软件的框架，里面包含main.c；module 文件夹用于编写新增功能的模块，编译后产生.dll 文件，拷贝到dll 文件夹，方便给主框架调用；
8、工程开发完成后打包成.exe 安装包，并添加版本号；便于发布；
9、每次开发后填好checkout point，并提交git，添加tag 并推送到远端仓库gitee_origin 和git_hub 远端仓库，便于checkpoint 管理；
10、每次BMAD执行完全部任务，请用windows 发出语音，“任务执行完毕”，提示我来检查；
11、软件打包好之后，发布到云服务器上去，www.opendebugger.com，目录为：D:\OpenDebugger，里面包含request.md
12、readme.md 内容写入帮助文档，F1键弹出帮助文档，帮助菜单下也添加一个帮助文档选项
13、增加stlink支持，stlink驱动安装目录在C:\Program Files\STMicroelectronics\STM32Cube\STM32CubeProgrammer\api\lib 下的CubeProgrammer_API.dll
	参考：https://github.com/DigitalAllianceStudio/HSS_DataVisualizer 项目支持 stlink 和jlink
	STM32G431RB nucleo 已经接入本电脑，可以随时查看；
	开发完成后，进行stlink 的全量测试，修复bug，回归测试、单元测试都通过后结束任务；

## 新增特性：
1、关闭软件时，记录软件的页面布局，方便下次启动软件后恢复上次调试界面；
2、布局文件可以单独另存为一个布局文件，方便分享给其他人使用；
3、tab里添加不同的窗口，和新建若干个 tab，方便变量观测和标定、修改，tab 需要支持重命名，重命名不需要弹出窗口，直接在标签上支持修改，如果没有命名/删除全部字符，则默认名称 Default，需要支持双击开启重命名；
4、icon文件夹里有app 的图标；
5、连接按键不需要弹窗，和选择MCU型号，仿真接口选择，时钟速度设置，还有jlink设备列表、刷新，这些直接放在界面上即可；
6、关于标签删除，只在帮助文档里写上：晶圆上的生物技术开发和提供支持，再添加版本号，网址：www.opendebugger.com
7、波形窗口支持鼠标滚轮缩放X轴和Y轴；停止记录后将波形整体展示到整个波形区域；CTRL+B 多坐标轴，f全局显示波形（参考vector CANape 软件图形窗口快捷键操作）；
8、SWD或者JTAG速度需要有更多速度设置，或者支持手工写入时钟速度；
9、波形窗口：多个变量同时采集，数值一直是0；
			添加变量到波形窗口，需要支持移除；
			CTRL+b 多Y坐标显示功能没有实现（坐标轴颜色跟随变量颜色）左边的elf解析后的变量栏支持自动隐藏和钉住，与右边窗口支持中间分隔数表调整窗口大小；

10、数值窗口：值写入，直接在值一栏写进去即可，不需要弹窗，鼠标Enter一次写入后不再写入，变量前面增加选择框，用于表示是否实时更新，方便用户观测；
11、不管波形窗口还是数值窗口，都支持窗口最大化，填充满当前tab，缩放功能；一个tab里可以同时添加多个波形/数值窗口，这样方便调试；
12、左侧的elf窗口请添加钉图标和自动隐藏功能（没有钉住就自动隐藏）；
13、波形窗口：
	右键添加变量，弹窗模糊搜索后，下面变量列表，需要支持ctrl+a全选，需要支持按住ctrl 鼠标单击变量多选，和按住shift，鼠标选择起止变量中间的全部变量（包括起止变量本身）；
	多坐标轴放到，界面左边；
	ctrl+b 将所有信号一行一行单独排列，再按ctrl+b，恢复到全部一起排列，这样方便查看；
	波形上的采样点放大后需要用圆点表示出来，这样方便我看采样间隔和具体采样时间；
	增加光标功能（XY向都添加），可以用于计算Δ值，增加鼠标点在某个采样点上，显示数值功能（变量名，值，数据类型）；增加同一个X轴分别将Y轴各个曲线数值同时显示出来的功能，方便同一个时间查看各个变量的值；
14、下面的消息栏需要支持上下拉伸，方便调整各个区域展示的内容；
16、tab 和右侧的空白处右键需要支持新建tab（波形/数值/示波器窗口）；
17、软件的每次弹窗让我选芯片很烦人，是否可以跳过选芯片环节，只需要选芯片核心，比如：Cortex-M0+；如果软件只是SWD/JTAG标准协议读写内存/读取Flash地址内容，是否可以不需要知道芯片型号和芯片架构？
18、示波器窗口没有什么作用，请删除整个示波器功能模块；
19、数值窗口：右键添加变量，弹窗模糊搜索后，下面变量列表，需要支持ctrl+a全选，需要支持按住ctrl 鼠标单击变量多选，和按住shift，鼠标选择起止变量中间的全部变量（包括起止变量本身）；
20、软件增加设置，整体界面风格分为白色（默认）和黑色（配色参考黑色IAR或者Notepad++ 黑色风格）；
21、避免弹窗设置芯片型号，只是读取变量，不需要知道具体芯片型号，“Device Selection”或者“Target device setting” 弹窗请不要触发；
22、elf 解析后的变量列表窗口需要实现抽屉缩起来和弹出；类似vscode 各个子窗口都可以调整，下面的消息窗口也是类似，需要能全部调整缩起来和拉出；
23、仔细实现：波形窗口鼠标缩放功能存在跳变，保持现有功能情况下，需要实现更丝滑和连续的拉伸、拖拽波形，新增鼠标框选局部区域放大功能，方便调试；


## Bug:
1、添加2个变量后App异常退出；
2、正常关闭软件后，再打开，任务栏里有OpenScope图标，但整个软件页面不能显示出来；
3、一个标签页里没有实现窗口缩放和最大化最小化，一个标签页现在还不能支持同时放置多个波形窗口和数值窗口请修复，请增加单独窗口全屏/推出全屏功能；
4、左侧的elf变量列表：
	没有实现自动隐藏和钉住功能；
	需要增加按下ctrl鼠标连续选择并右键添加到窗口（波形/数值/示波器）；
	
5、波形窗口：内部文字波形窗口1 去掉，不需要；
	波形放大后，曲线上的采集点用圆点表示出来没有实现（一开始有的，后面随着波形录制时间变成，全部圆点都消失了）；
6、tab内部的窗口没有实现最大化、最小化、鼠标拉伸边沿，也没有实现一个tab 装几个的窗口功能；
7、波形在采集的时候，点击录制数据，界面会卡死在选择录制文件路径上；
8、自动隐藏功能是指的，左侧的elf变量列表，而不是菜单栏下面的固定按键；
9、除了4000以外的速度连接，其余速度连接采集到的变量值都是0；
10、现在用12000的速度采集数据，一开始就会失败；采集线程退出，读取xxx变量失败；
11、采集线程提示退出，鼠标在波形区域滑动的时候，整个波形界面都在闪烁；
12、左侧的elf变量区域：
	自动往左边界隐藏功能没有实现；
	变量选择需要按下shift 鼠标点击起始变量，一直到鼠标再次点击的范围内的全部变量，ctrl按下后鼠标点击连续选择变量；
13、下面的消息区域：	
	按下shift 鼠标点击起始消息，一直到鼠标再次点击的范围内的全部消息，ctrl按下后鼠标点击连续选择消息；需要支持右键菜单复制，和全部清除；
14、软件还是弹窗选择芯片，并闪退；
15、我看了一下波形采样点的间隔是20ms以上，太慢了，如何实现更高速度的采样？
	  采集线程 datasrv.c:30-85 的循环结构：

  while (!g_app.stop_poll) {
      检查连接 → 对每个观测变量各发一次 OS_CMD_READ_MEM → 推入 ring → Sleep(20ms)
  }

  瓶颈 1：固定 Sleep(20ms)（最致命）
  main.c:164 把 poll_interval_ms 硬编码为 20。即使每次读取只要 0.1ms，周期也被强行拉长到 20ms → 50Hz。

  瓶颈 2：每个变量一次独立 USB 读
  datasrv.c:45-60 对每个观测叶单独调一次 OS_CMD_READ_MEM → JLINKARM_ReadMem（jlink.c:290）。每次调用都有 USB 往返的固定开销（约 50~200µs）。N 个变量 = N 次
  USB 事务。所以变量越多，单周期越慢——这就是为什么多个变量时 20ms 都未必够。

  瓶颈 3：UI 刷新频率
  datasrv.c:152 每批样本 PostMessage(WM_OS_SAMPLES) → 主线程把所有窗口重绘一遍。高速下会刷爆 UI 消息队列。

  物理上限：J-Link 轮询能到多少？

  - 单变量、去掉固定 Sleep 自由跑：每个样本 ≈ 100~200µs（USB 往返主导）→ 约 5~10kHz
  - 多个变量合并成一次连续块读取：同样 ≈ 100~300µs → 3~10kHz
  - 真·µs（1MHz 采样）在 J-Link 轮询路径上做不到——USB 往返 ≥50µs 就是硬墙。

  实现路线图

  第一步：软件快采样（可达 ~10kHz，几十到几百 µs/样本）
  1. poll_thread 去掉固定 Sleep，改成"读完立即下一周期"（可选保留最小间隔防刷爆 USB）
  2. 批量块读：把观测变量按地址排序，连续区间合并成一次 ReadMem，回来后按偏移切片——把 N 次 USB 事务压成 1 次
  3. WM_OS_SAMPLES 限频：ring 照常灌满，但 UI 只按 30~60Hz 重绘（波形历史本来就在 ring 里，不受影响）

  第二步：真 µs 采样（片上缓冲，XCP 式）后续版本再实现，当前版本不开发
  1. 目标 MCU 上跑一段采样代码（定时器 ISR 把变量抓进 RAM 环形缓冲，µs 级间隔）
  2. 主机每 10~50ms 用一次 ReadMem 整块下载缓冲（比如 64KB = 一次抓几万点 µs 间隔样本）
  3. 这是 CANape/INCA 做"快速采集"的标准做法，J-Link 轮询路线到不了这个量级
16、软件添加6个变量之后，闪退了，请检查原因并修复，着重看内存管理；
17、安装包开始安装的时候，界面还是显示的 v1.13.0，但安装后的帮助-关于里的软件版本是 v1.14.0 正确；
18、还是没有实现全部隐藏全局变量窗口功能；
19、同一个标签页的多个窗口，总是只有第一个窗口更新/绘图，依次关闭最前面的窗口，后面的窗口才能继续更新，请解决这个问题，需要实现同时更新一个标签页的全部变量；


## 报错：
1、消息窗口，（配置成 SWD 12000KHz时钟情况下）：
04:46.566 警告 采集线程已退出
04:46.565 错误 采集停止：MCU 连接已断开
04:46.564 错误 J-Link 连接失败: JLINKARM_Open 失败 (rc=-488389840)
04:46.564 错误 J-Link 连接: JLINKARM_Open 失败 rc=-488389840
04:46.562 信息 J-Link 连接: device='Cortex-M4' iface=SWD speed_khz=12000 probe_idx=-1 serial='(空)'
04:46.561 警告 J-Link 连接: 显式选择仿真器失败，改用自动选择重试
04:46.561 错误 J-Link 连接: JLINKARM_Open 失败 rc=-488389840
04:46.547 信息 J-Link 连接: device='Cortex-M4' iface=SWD speed_khz=12000 probe_idx=0 serial='(空)'
04:46.546 信息 J-Link 已断开
04:46.054 错误 块读 @0x20000394 失败 (ret=-5, 1 变量)
04:46.054 错误 块读 @0x20000394 失败 (ret=-5, 1 变量)
04:46.054 错误 块读 @0x20000394 失败 (ret=-5, 1 变量)
04:46.053 信息 J-Link 已连接: J-Link Pro V4 compiled Sep 22 2022 15:00:37



## 具体实现一个业务模块：
1、扫描usb 外设，找到Jlink 相关的外设，如果没有找到，弹窗用户没有发现Jlink设备，UI界面可以支持用户选择仿真口类型：SWD/JTAG，以及时钟速度；实现掉用 dll 文件夹下的JLink_x64.dll，用户连接MCU后，读写MCU 内部的变量，数据需要添加读取时刻的时间戳，方便绘图和后处理；
2、参考链接：
https://www.segger.com/products/debug-probes/j-link/tools/j-link-sdk/
https://github.com/shadowthreed/JLink_API
https://cloud.tencent.com/developer/article/2232891

3、Jlink驱动安装目录：C:\Program Files\SEGGER\JLink_V966


